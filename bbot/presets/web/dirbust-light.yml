description: Basic web directory brute-force (surface-level directories only)

include:
  - iis-shortnames

modules:
  - web_brute

config:
  modules:
    web_brute:
      # wordlist size = 1000
      lines: 1000
