description: Basic web directory brute-force (surface-level directories only)

include:
  - iis-shortnames

modules:
  - webbrute

config:
  modules:
    webbrute:
      # wordlist size = 1000
      lines: 1000
