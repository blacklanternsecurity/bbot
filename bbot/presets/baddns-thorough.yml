description: Run all baddns and baddns_zone submodules.


modules:
  - baddns
  - baddns_zone
  - baddns_direct

config:
  modules:
    baddns:
      enabled_submodules: [NSEC,CNAME,references,zonetransfer,MX,NS,TXT]
