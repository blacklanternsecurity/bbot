description: Everything everywhere all at once

include:
  - subdomain-enum
  - cloud-enum
  - code-enum
  - email-enum
  - spider
  - web
  - paramminer
  - dirbust-light
  - web-screenshots
  - baddns-heavy

config:
  modules:
    baddns:
      enable_references: True
    wayback:
      urls: True
      parameters: True
      archive: True
